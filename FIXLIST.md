# Vocare Fix List

A flat, non-narrative punch list of bugs/gaps found after the fact — outside a formal build session, or spanning modules a single `tasks.md` wouldn't cover. Not for deferred product decisions (those go in the spec's Section 24 Parking Lot) and not for the story of *how* something was found (that's `journal/`). Just: what's wrong, where, status.

Format: `- [ ]`/`- [x]`, module(s) touched, one line on what's wrong, date found/resolved.

## Open

- [ ] **M1** — Signup screen copy (DOB privacy line, AI-disclosure checkbox text) reads at a higher verbosity/comprehension level than plain-language guidance would recommend. Not wrong, just risks being skimmed past or misread by some users. Reword in a future pass — not urgent, flagged 2026-07-22. Touches `web/src/pages/SignUpPage.tsx`.
- [ ] **M1 — action item, not a bug.** Robin wants to give a specific friend unlimited Vocare use. Mechanism: once the friend signs up through the real magic-link flow, manually set his `entitlement_status` in production Postgres — no code change needed. Note: M1's fair-use velocity cap still applies regardless of paid status by design, so "unlimited" is bounded by that cap unless he's also given the crisis-style exemption. **This is a production auth/entitlement change (rule 5) — requires Robin's explicit go before executing, not just this note.** Flagged 2026-07-22.

- [ ] **M1** — `SignUpPage.tsx` calls `navigate("/account")` directly in the render body (`if (session) { navigate(...) }`) instead of inside a `useEffect`. Same anti-pattern that surfaced as a real "Cannot update a component while rendering a different component" warning in the new `AppShell.tsx` (M2.1) once `AppShell` was fixed to use `useEffect` properly instead. `SignUpPage.tsx`'s copy wasn't touched — out of scope for the M2.1 change that found it — but it's the same bug, just not yet observed to warn in practice. Flagged 2026-07-22.
- [ ] **M2 — action item, not a bug.** Evaluate ThroughLine (throughlinecare.com, the company behind findahelpline.com — the crisis-safety net's current generic fallback link) as a possible long-term replacement for the hand-curated `CRISIS_RESOURCES` map. Their API/widget covers 170+ countries, "vetted and verified... maintained daily," with region-level refinement — would resolve the region/language granularity gap (see Section 24) and the staleness risk of a hand-maintained list. Checked 2026-07-22: no public pricing, no free tier confirmed — real fees are set per a negotiated "service order," access requires signing up for a developer account and being manually enabled by their team, standard SaaS liability/termination terms (their liability capped at 12 months' fees paid; scraping/reverse-engineering explicitly prohibited by their terms, consistent with the 403s hit trying to fetch findahelpline.com directly). This is a real vendor relationship requiring a sales conversation, not a quick integration — needs an actual pricing conversation before any decision. Flagged 2026-07-22, not pursued further this session.

- [ ] **M3 (voice capture) — final real-device re-verification still pending.** Fixed across three iterations on 2026-07-23 (`660a62c`, `a0dadcd`, `a8a5aed` — each corrected a wrong guess about the actual mechanism, ending at `mergeSpeechSegments` in `web/src/lib/`, which collapses SpeechRecognition entries that cumulatively restate the whole utterance so far, without ever collapsing a genuinely repeated word). Automated tests cover the confirmed repro shapes (Android Pixel 10's "I'm/I'm going/I'm going to/..." restatement pattern, and the "test test test" over-collapse regression the second attempt introduced), but Robin hasn't been able to do a final live re-test yet — hit the M1 velocity cap (`FAIR_USE_CAP.per24h: 6`) from the same day's repeated test sessions. Two of the oldest test sessions were deleted directly from production to clear headroom, but a fresh real-device check (fast speech on the Pixel 10 *and* an intentional repeated word on the laptop) is still owed once Robin has a session available. Touches `web/src/pages/ConversationPage.tsx`, `web/src/lib/merge-speech-segments.ts`.
- [ ] **Web (M1/M2/M2.1) — test-coverage gap.** The web workspace has 2 test files total (`App.test.tsx`, one smoke test; `ConversationPage.test.tsx`, the crisis-card `href` regression test) versus backend's 7 files/42 tests. Everything built in the 2026-07-22 nav-shell/theme/date-input session — `AppShell`'s auth-guard redirect, the theme toggle's persistence, `SegmentedDateInput`'s date-validation logic, sign-out — has zero automated coverage; each was verified via lint/typecheck/build plus a live look, not an automated test. Riskiest pieces to leave uncovered: `SegmentedDateInput`'s `isRealDate` round-trip check (real edge-case logic — Feb 30, leap years) and `AppShell`'s auth-guard (a break here could show signed-out users pages they shouldn't see, or bounce signed-in users incorrectly). Flagged 2026-07-22, not yet acted on.

## Resolved

- [x] **M1** — `mockups/interface-v1.html`'s signup screen had drifted from the real built app: showed one bundled checkbox ("I'm 16 or older...") where the real `SignUpPage.tsx` already used a separate DOB field (age, computed/enforced server-side) plus a distinct AI-disclosure checkbox. Fixed 2026-07-22 by matching the mockup to the live code exactly — including field order (Email → DOB → Country → fine print → checkbox), which needed a second pass after the first fix reordered fields and invented a line not present in the real code.
