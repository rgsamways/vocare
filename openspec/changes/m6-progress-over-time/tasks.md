## 1. Database schema (rule-5 sensitive: migration + deploy)

- [x] 1.1 Add `anchorRevisions` table to `backend/src/db/schema.ts`: `id` (uuid, pk, default random), `anchorId` (uuid, FK to `anchors.id`, not null), `label` (text, not null), `targetRole`/`targetIndustry`/`jobDescriptionText`/`company` (nullable text, matching `anchors`' own columns), `revisedAt` (timestamp, not null, default now) — snapshot of the anchor's pre-edit values, per design.md's Decisions
- [x] 1.2 Generate migration via `drizzle-kit`, apply against local Postgres (Docker, port 5434), confirm the table and FK constraint exist — **done: `backend/drizzle/0006_minor_groot.sql`, applied to local Postgres, table + FK confirmed via `information_schema`**
- [x] 1.3 **Confirm with Robin before proceeding** — applying the migration against production Postgres (via the `Postgres` service's public proxy, `DATABASE_PUBLIC_URL`, never the internal-only `DATABASE_URL`) and deploying to Railway (`railway up --service vocare`) are rule-5 sensitive; this module is also two-instance (CLAUDE.md, confirmed 2026-07-23) — **Robin confirmed go-ahead 2026-07-23, after the account-deletion-cascade gap below was raised and resolved.**

## 2. Anchor management endpoints

- [x] 2.1 Create `backend/src/routes/anchors.ts`, registered in `app.ts` alongside the other route modules
- [x] 2.2 `GET /anchors` — list the caller's own anchors, active-only by default, `?includeArchived=true` to include archived ones; ownership-scoped by `userId` from the auth session
- [x] 2.3 `PATCH /anchors/:id` — ownership-checked (reuse `conversation.ts`'s `loadOwnedAnchor` pattern); if any of `label`/`targetRole`/`targetIndustry`/`jobDescriptionText`/`company` differ from current values, insert an `anchor_revisions` row with the pre-edit snapshot before applying the update; no-op edit (no field changes) writes no revision
- [x] 2.4 `POST /anchors/:id/archive` and `POST /anchors/:id/unarchive` — ownership-checked, set/clear `archivedAt`; no revision row written for either
- [x] 2.5 `GET /anchors/:id/revisions` — ownership-checked, returns that anchor's `anchor_revisions` rows ordered oldest-first
- [x] 2.6 Unit tests: edit-with-change writes exactly one revision with correct pre-edit values; edit-with-no-change writes no revision; archive/unarchive toggle `archivedAt` without writing a revision; default `GET /anchors` excludes archived, `?includeArchived=true` includes them; non-owner requests to all five endpoints are denied as if the anchor didn't exist — **done in `backend/src/routes/anchors.test.ts`**

## 3. Session history endpoints

- [x] 3.1 Create `backend/src/routes/progress.ts`, registered in `app.ts`
- [x] 3.2 `GET /sessions` — the caller's own completed (`status: "complete"`) sessions, most recent first, each with enough summary data (e.g. `createdAt`, linked anchor label if any) to render a list without a second round-trip
- [x] 3.3 `GET /sessions/:id` — ownership-checked, returns that session's full `transcript_turns`, its `feedback_reports` entry, and its linked anchor's label if any; 404s (denied-as-nonexistent) for incomplete or unowned sessions
- [x] 3.4 Unit tests: history list excludes incomplete sessions; session detail returns transcript + feedback report together for an owned completed session; non-owner and incomplete-session requests to `GET /sessions/:id` are both denied as if the session didn't exist — **done in `backend/src/routes/progress.test.ts`**

## 4. Progress trends endpoint

- [x] 4.1 `GET /progress/trends` on `progress.ts` — computed on-demand (no new stored table), per design.md's Decisions
- [x] 4.2 Tradeoff-reasoning trend: compare `tradeoff_reasoning_present` between the caller's most recent completed session and the completed session exactly 3 before it (across all their completed sessions); omit from the response if fewer than 4 completed sessions exist
- [x] 4.3 Audience-alignment trend: compare `audience_keyword_matches[]` length between the most recent completed session linked to a given anchor and the completed session linked to that same anchor exactly 3 before it; omit per-anchor if fewer than 4 qualifying sessions exist for that anchor, or if either side's `audience_keyword_matches` is null
- [x] 4.4 Both trends report backward movement in the same plain, non-judgmental register as forward movement — no silence standing in for decline (spec's 2026-07-22 requirement)
- [x] 4.5 No filler-word trend field anywhere in the response shape
- [x] 4.6 Unit tests: 4+ sessions with a true→false or false→true flip 3 apart produce the correctly-worded trend in both directions; fewer than 4 sessions omits the trend; per-anchor audience trend is scoped correctly when a user holds more than one anchor; no anchor linkage produces no audience-alignment trend entry; response contains no filler-word key under any input

## 5. Web UI — anchor management

- [x] 5.1 Anchor list/management screen: active anchors by default, toggle to show archived; edit form (label/target role/target industry/job description/company) calling `PATCH /anchors/:id`; archive/unarchive action calling the two new endpoints
- [x] 5.2 Revision history view for an anchor: renders `GET /anchors/:id/revisions` chronologically, diffing consecutive snapshots (and the live anchor as the final "state") client-side into a plain-language change description (e.g. "narrowed from 'any backend role' to 'backend infra, fintech'"), per design.md's Decisions — no chart, plain text list
- [x] 5.3 Accessibility: semantic HTML/ARIA labeling for the edit form, archive controls, and revision list, matching M5's existing screen-reader posture

**Implemented as `web/src/pages/ProgressPage.tsx`** (anchor management + session history/trends combined into one page, per `cli`'s report — not split into separate files; note for the independent grader to confirm this still satisfies 5.x/6.x as written).

## 6. Web UI — session history & trends

- [x] 6.1 Session history list screen, reading `GET /sessions`, each entry opening a detail view (`GET /sessions/:id`) showing the full transcript and that session's feedback report
- [x] 6.2 Trend summary view reading `GET /progress/trends`, rendered as plain text sentences (both improvement and decline framed identically in tone) — no chart/graph component introduced
- [x] 6.3 Accessibility: trend direction and history content exposed as readable text/semantic structure, not conveyed only by color or icon

## 7. Verification

- [x] 7.1 `npm run lint|typecheck|test|build --workspaces --if-present` clean across all workspaces — **128 tests passing (111 backend, 17 web)**
- [x] 7.2 Manual (local): edit an anchor's fields twice, confirm two `anchor_revisions` rows exist with the correct pre-edit snapshots and the live row reflects only the latest edit
- [x] 7.3 Manual (local): archive then unarchive an anchor, confirm `archivedAt` toggles and no revision rows are added by either action
- [x] 7.4 Manual (local): with 4+ completed sessions (some linked to an anchor, some not), confirm the trends endpoint reports both trend types correctly in both directions, and omits them when history is short
- [x] 7.5 Manual (local): open session history for a real completed session, confirm the full transcript and feedback report render together
- [x] 7.6 **Confirm with Robin before proceeding** — production migration + Railway deploy (task 1.3); after go-ahead, apply and verify against real production data (a real anchor edit produces a real `anchor_revisions` row; real session history renders for an existing production user) — per CLAUDE.md's Deployment note, this is part of the module being "done" — **Robin confirmed go-ahead 2026-07-23, run after task 8 (done above). Migration applied 2026-07-23 via `DATABASE_PUBLIC_URL` (confirmed `anchor_revisions` table + FK via `information_schema`/`pg_constraint` against production, matching local). `railway up --service vocare` deployed; confirmed live via `/health` (200) and `/anchors`, `/sessions`, `/progress/trends` all returning 401 (auth-gated, not 404) — same live-vs-404 proof M4/M5 used. Real-user verification (an actual anchor edit producing a real `anchor_revisions` row; real session history rendering) needs Robin to perform the live action directly, matching M4/M5's exact precedent ("Robin completed one real production session directly") — `cli` doesn't have and won't fabricate a real user's auth session. Handed back to Robin for that step.**
- [ ] 7.7 Independent grading instance reviews this change from the persisted proposal/design/specs/tasks files (not shared conversation context) before/alongside `cli` applying it, per the confirmed two-instance process for this module

## 8. Account-deletion cascade fix (added 2026-07-23, raised by `cli` mid-implementation)

`delete-user-cascade.ts`'s own comment names `anchors`/`anchor_revisions` as tables to be added to the cascade "by whichever module creates each one" — M6 creates `anchor_revisions`, so this closes that gap now rather than tracking it separately (**Robin's call, 2026-07-23**, over leaving it in FIXLIST).

- [x] 8.1 Add explicit `anchors` and `anchor_revisions` deletes to `deleteUserCascade` in `backend/src/account/delete-user-cascade.ts`; delete `anchor_revisions` rows before `anchors` rows (FK order) — **deviation from the instruction's literal ordering, verified before writing: `sessions.anchor_id` has its own `ON DELETE no action` FK into `anchors.id`, checked per-statement (not deferred) even inside the same transaction — confirmed by hand against local Postgres that deleting an anchor while a session still references it raises a live FK violation. So the anchor/anchor_revisions deletes run *after* the existing `sessions` delete, not before, with the reasoning recorded in the file's own Decisions comment. `anchor_revisions` still deletes before `anchors` as asked.**
- [x] 8.2 Unit test: deleting a user with an anchor that has revision history removes both the `anchor_revisions` rows and the `anchors` row, alongside everything `delete-user-cascade.test.ts` already covers — **done: seeded anchor now also carries a revision row and a linked session (to actually exercise the FK-ordering question above, not just assert the happy path); both pass**
- [x] 8.3 Re-run `npm run lint|typecheck|test|build --workspaces --if-present` after this addition before proceeding to 7.6's production deploy

## 9. Mockup parity fix (added 2026-07-23, found by Robin live-testing production)

`mockups/interface-v1.html`'s `#progress` screen (lines 349–392) was already settled before this module existed, the same way M2's conversation-screen mockup was — but it wasn't checked when writing this change's design/tasks, so groups 5/6 shipped generic styling reused from the feedback page instead. Robin compared the live site to the mockup directly and flagged it. See design.md's 2026-07-23 correction for the two decisions this closes.

- [x] 9.1 Add a short first-user-turn preview to `GET /sessions`'s response (`progress.ts`): for each session, the first `transcript_turns` row where `speaker: "user"`, truncated (~80–100 chars, word-boundary-safe, with an ellipsis if cut), as a new field (e.g. `topicPreview: string | null`; `null` for a session with no user turns, which shouldn't happen for a completed session but shouldn't throw if it does) — **done: one batched query for all sessions' first user turn (not N+1); 90-char word-boundary truncation with `…`**
- [x] 9.2 Update `web/src/index.css`: port the mockup's `.trend-line`, `details.history-row` (+ `.history-note-wrap`/`.history-date`/`.history-note`/`.history-expanded`/`.chevron-icon`), `.anchor-card`(`.active`)/`.anchor-revision`(`.earned`), and `.add-anchor-link` rules (mockup lines ~160–189); add `--warm`/`--warm-soft` to both the light and dark theme variable blocks (mockup's light values: `#b8703f`/`#f6e8dc` — choose reasonable dark-theme equivalents consistent with how the existing `--accent`/`--accent-soft` pair differs between the two blocks, not a direct copy) — **done, with two deviations from a literal port, both deliberate: (1) the mockup's lucide `<i data-lucide>` icon markup is not ported — no existing page in this app (FeedbackPage, AccountPage) uses an icon library, so this keeps that same text-only precedent rather than introducing one just for this screen; (2) added a `--warm-ink` variable pair (light `#7a4a22` matching the mockup's hardcoded color, dark `#e8b98a`) since the mockup hardcodes `.anchor-revision.earned`'s text color rather than variablizing it, which would render illegibly in dark mode otherwise — same `-bg`/`-ink` pairing this file already uses for `warn`/`safe`. Dark `--warm`/`--warm-soft` chosen as `#d98a4f`/`#3d2c1c`.**
- [x] 9.3 Restyle `ProgressPage.tsx`'s trend section to use `.trend-line` rows (icon + message), matching the mockup's two example rows — **done, text-only (see 9.2's icon-omission note — no icon glyph, same precedent)**
- [x] 9.4 Restyle the session history list to use native `<details className="history-row">`/`<summary>` per session (replacing the current `<button>`-per-row pattern), showing `topicPreview` (task 9.1) in place of the mockup's fixed "Talked about X" example text as the visible label — real quoted content, not literal mockup copy — plus the existing date and anchor label; expanded content (transcript bubbles + feedback notes) stays as already implemented, just inside the restyled wrapper — **done; falls back to "Session on {date}" when `topicPreview` is null (the documented shouldn't-happen case)**
- [x] 9.5 Restyle anchor cards to use `.anchor-card`(`.active` when not archived)/`.anchor-revision.earned`: show the single most-recent revision's diff inline on the card automatically (not gated behind "View history"), matching the mockup's always-visible callout; keep the existing "View history" affordance for the full chronological list as an addition beyond what the mockup shows, styled consistently with the rest of the app rather than copying mockup markup that doesn't exist for it — **done; anchor revisions are now prefetched for every anchor when the list loads (not lazily on first "View history" click) so the inline callout is immediately visible, per the mockup's always-visible treatment**
- [x] 9.6 Add an "Add another anchor" affordance matching `.add-anchor-link`, if creating an anchor from this page wasn't already possible — check first; M2's `POST /anchors` may only be reachable from session start today, in which case this is a new (small) link/flow, not just styling — **checked: confirmed `POST /anchors` was only ever called from `ConversationPage.tsx`'s session-start form (per M2), not reachable from Progress at all. Built the small new flow: `.add-anchor-link` reveals an inline create form (same 5 fields as edit, factored into a shared `AnchorFieldset` component to avoid duplicating the edit form's ~40 lines) that POSTs to the existing `POST /anchors` and refreshes the list on success. No new endpoint — reuses M2's.**
- [x] 9.7 Unit/type checks: `progress.test.ts` covers `topicPreview` (present for a normal session, `null`/absent handled gracefully); full workspace `lint|typecheck|test|build` clean — **done: 2 new backend tests (113 total, up from 111); full workspace lint/typecheck/test/build clean. Also visually verified locally in a real browser (Playwright, driven manually — not part of the repo) against a seeded local user with real anchors/revisions/sessions: light + dark theme both screenshot-checked, session-row expand/collapse and archived-anchor-card states interacted with directly, zero console errors. Scratch script + seeded test data both deleted afterward, not committed.**
- [x] 9.8 Manual: reload the live production Progress page, confirm it visually matches the mockup's `#progress` screen's layout and that each session row now shows distinguishing real content instead of a bare repeated date — **closed by Robin directly (real browser, `vocare.ca/progress`): topicPreview text and restyled trend/history sections confirmed live and matching. Anchor card styling flagged as a separate follow-up — see Group 10.**

## 10. Anchor card polish (added 2026-07-23, Robin flagged live on production)

Robin loaded `vocare.ca/progress` in a real browser for 9.8 and flagged the anchors section specifically. The mockup's anchor card (lines 382–391) shows no action buttons at all (it's static) — 9.5 left "style the actions consistently" as an open call since the mockup doesn't show them, and what shipped was three full-width, stacked, heavy-bordered buttons that dominate the small card and clash with the mockup's minimal feel. Two fixes, confirmed with Robin, plus one data check:

- [x] 10.1 Replace the 3 stacked full-width Edit/Archive/View-history buttons with a compact horizontal row of lighter text-link-style actions (similar visual weight to `.add-anchor-link`'s minimal treatment, not full `.btn.secondary` boxes) — **done: new `.anchor-card-actions` CSS (flex row, gap, borderless text-link buttons); visually re-verified locally (Playwright screenshot) before deploying**
- [x] 10.2 Add a status line under the anchor label ("Active" or "Archived" — the mockup's "· linked to today's session" detail is optional, skip if it needs data not already on hand) and apply the mockup's `.anchor-card.active` accent-colored border to non-archived anchor cards — **status line done (was previously overwritten by the role/industry text — now both render as separate lines). The `.active` border class logic was already correctly applied (`anchor-card${anchor.archivedAt ? "" : " active"}`) — not a bug, just harder to notice under the heavy buttons 10.1 just removed; confirmed clearly visible in the re-screenshotted local check**
- [x] 10.3 Check (don't blindly patch) the stray space before the comma in an anchor's subtitle (observed as "Senior developer , Automotive") — determine whether it's a trailing space in that anchor's stored `target_role` data (a pre-existing test artifact, not a bug) or an actual join-logic issue; report which before changing anything — **checked directly against production: `target_role` is literally stored as `"Senior developer "` (17 chars, trims to 16) — a real trailing space in the data itself, not a join-logic bug (`[role, industry].filter(Boolean).join(", ")` behaves correctly; the extra space was already in the stored value before the join ran). Matches M4/M5's own documented "Senior developer" test fixture from earlier module verification. Not patched, per instruction.**
- [x] 10.4 Re-verify against production after deploying (commit → push → Vercel auto-deploy, same path as Group 9) — **committed as `8e0c668`, pushed to `main`; confirmed via `vercel inspect` that the resulting deployment is aliased to `https://vocare.ca` (live). Closed by Robin directly (real browser, `vocare.ca/progress`): confirmed the anchor actions now read as a compact horizontal row and the Active/Archived status line plus accent border are visible. Group 10 done.**

## 11. Existing-anchor picker at session start (added 2026-07-23, gap found explaining anchors to Robin)

`ConversationPage.tsx`'s session-start "anchor" toggle only ever calls `POST /anchors` to create a brand-new anchor (confirmed at `ConversationPage.tsx:168-187`) — there's no way to link a new session to an anchor that already exists, so every session with the toggle checked creates another anchor instead of reusing one. Since M6 is "where anchor management lives" and just built `GET /anchors`, Robin's call (2026-07-23) is to close this now rather than leave it for later. See design.md's second 2026-07-23 correction.

- [x] 11.1 On `ConversationPage.tsx`'s session-start form, fetch `GET /anchors` (active only — an archived anchor is retired, not a session target) and, when the user has at least one, offer a choice: pick an existing anchor, create a new one (existing inline form, unchanged), or none — accessible control (e.g. a labeled radio group or select), not just a bare toggle — **done: a labeled `<select id="anchor-choice">` (same pattern as the existing persona pickers), fetched once on mount into `existingAnchors`**
- [x] 11.2 When an existing anchor is picked, skip the `POST /anchors` call entirely and pass that anchor's `id` straight to `/sessions/start`'s `anchorId`; "create new" keeps the current create-then-use-new-id flow; "none" keeps current no-anchor behavior — **done via `pickedAnchorId`/`creatingNewAnchor` derived from `anchorChoice`**
- [x] 11.3 When the user has zero anchors, skip the picker entirely and keep today's create-or-none toggle as-is — no empty picker UI for a first-time user — **done: `hasExistingAnchors` gates which of the two UIs renders; zero-anchor path is untouched (still the original checkbox)**
- [x] 11.4 Unit tests (`ConversationPage.test.tsx`): picking an existing anchor sends that `anchorId` without calling `POST /anchors`; creating a new one still calls it as before; a user with zero anchors sees the original toggle, not a picker — **done: 3 new tests, all passing (20 web tests total, up from 17); updated the 3 pre-existing fetch mocks in this file to also handle `GET /anchors` (now called unconditionally on mount) so they didn't start throwing "Unexpected fetch"**
- [x] 11.5 Full workspace `lint|typecheck|test|build` clean; manual check locally that starting a session against a previously-created anchor doesn't create a duplicate — **done: full workspace clean (113 backend + 20 web tests). Manual check via a real browser (Playwright) against local dev servers and a seeded user with one existing anchor: picked it from the dropdown, started a session, confirmed via direct DB query that exactly one `anchors` row still exists for that user and the new session's `anchor_id` points to it — no duplicate created. Scratch script and seeded data deleted afterward.**
- [ ] 11.6 Re-verify against production after deploying (commit → push → Vercel auto-deploy)
