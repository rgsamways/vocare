## 1. Database schema (rule-5 sensitive: migration + deploy)

- [x] 1.1 Add `session_mining_results` table to `backend/src/db/schema.ts`: `sessionId` (uuid, FK to `sessions.id`, unique — one row per session), `ownershipLanguagePresent` (boolean), `tradeoffReasoningPresent` (boolean), `techDomainMentions` (jsonb string array), `clarity` (text enum), `sentiment` (text enum), `growthSignals` (jsonb string array), `outcomeMentioned` (boolean), `quantifiedImpactExamples` (jsonb string array), `audienceKeywordMatches` (nullable jsonb string array), `topicRelevanceScore` (integer), `minedAt` (timestamp, default now)
- [x] 1.2 Generate migration via `drizzle-kit`, apply against local Postgres (Docker, port 5434), confirm the table and constraints exist
- [ ] 1.3 **Confirm with Robin before proceeding** — applying the migration against production Postgres (via the `Postgres` service's public proxy, `DATABASE_PUBLIC_URL`, never the internal-only `DATABASE_URL`) and deploying to Railway (`railway up --service vocare`) are both rule-5 sensitive (database migrations, deployment config); this module as a whole is also under the two-instance chat/cli review CLAUDE.md names it for as of 2026-07-22
- [ ] 1.4 Apply the migration against production Postgres and deploy to Railway; verify against a real completed production session (confirm a `session_mining_results` row is written) — per CLAUDE.md's Deployment note, this deploy is part of the module being "done," not a later step

## 2. Role-language reference library

- [x] 2.1 Create `backend/src/mining/role-language.ts`: a small starter set of role categories (e.g. "backend engineer," "product manager," "designer," a generic fallback), each with a short list of representative terms/phrases — explicitly flagged as a placeholder pending real usage data, same convention as `CRISIS_RESOURCES`/`PERSONA_COMBINATIONS` in `config.ts`
- [x] 2.2 Add a matching function: given an anchor's `target_role` string, return the best-matching category's reference set, or `undefined` on no confident match (no nearest-guess fallback — see design.md's Decisions)
- [x] 2.3 Unit tests: exact/substring match returns the right category; an unrecognized `target_role` returns `undefined`, not a generic fallback

## 3. Extraction call

- [x] 3.1 Add `MINING_MODEL` to `config.ts` (Haiku 4.5, same as `CONVERSATION_MODEL`/`CRISIS_CHECK_MODEL`); add placeholder `OFF_TOPIC_THRESHOLD` and off-topic-session-count denial threshold alongside `FAIR_USE_CAP`, flagged as placeholders pending beta data
- [x] 3.2 Create `backend/src/mining/extract.ts`: a structured-output call (`output_config: { format: { type: "json_schema", schema } }`, same pattern as `crisis-safety.ts`) taking the full transcript (+ role-language snippet when an anchor/target_role match exists) and returning the extraction schema's fields — no `filler_word_count` field in the schema at all (see design.md's Decisions)
- [x] 3.3 Unit tests (mocked Anthropic client): extraction call returns all expected fields for a representative transcript; `audience_keyword_matches` is omitted from the request/response schema when no anchor/target_role match exists; a malformed/unparseable model response is handled without throwing (log + skip, matching `crisis-safety.ts`'s fail-safe posture)

## 4. Account deletion cascade

- [x] 4.1 Add an explicit `session_mining_results` delete (matching `delete-user-cascade.ts`'s existing explicit-order convention, not a DB-level `ON DELETE CASCADE` — see that file's own comment on why) to `deleteUserCascade`, ordered before the existing `sessions` delete
- [x] 4.2 Unit test: deleting a user with a completed, mined session removes its `session_mining_results` row along with everything `delete-user-cascade.test.ts` already covers
- [x] 4.3 **Note, not in scope to fix here:** `delete-user-cascade.test.ts` doesn't currently exercise a user with real `transcript_turns` rows, and `transcriptTurns.sessionId`'s FK has no `ON DELETE CASCADE` either — deleting a user with any completed session may already fail on that pre-existing FK today, independent of this change. Flag to Robin; do not silently fix as part of M4 (scope discipline, CLAUDE.md rule 2).

## 5. Mining orchestration and trigger

- [x] 5.1 Create `backend/src/mining/mine-session.ts`: `mineSession(sessionId)` — loads the session's transcript + anchor, calls `extractSessionSignals`, writes the `session_mining_results` row; catches and logs any failure without throwing
- [x] 5.2 Wire `/sessions/:id/end` in `backend/src/routes/conversation.ts`: after the existing status update, call `void mineSession(session.id).catch(...)` without awaiting it in the response
- [x] 5.3 Unit tests: ending a session triggers exactly one `mineSession` call for that session's id; the `/end` response is returned without waiting on it (mock `mineSession` with a delayed promise and assert response timing); a `mineSession` rejection does not surface as an error response or affect the session's `complete` status

## 6. Entitlement wiring

- [x] 6.1 Add `getAbuseSignal(userId)` to `backend/src/entitlement/entitlement.ts`: counts, within the existing rolling 24h/30d windows, sessions whose joined `session_mining_results.topicRelevanceScore` is below `OFF_TOPIC_THRESHOLD`; sessions with no mining result yet are excluded from the count (per specs/session-entitlement's scenario)
- [x] 6.2 Wire `checkEntitlement` to deny (reusing `VELOCITY_CAP_MESSAGE`, no new user-facing message) when the off-topic count crosses the configured threshold, checked alongside the existing velocity-cap logic
- [x] 6.3 Unit tests: a user with repeated below-threshold sessions is denied on their next start; a user with no off-topic sessions is unaffected; the denial message is identical to the ordinary velocity-cap message (no distinguishing text)

## 7. Verification

- [x] 7.1 `npm run lint|typecheck|test|build --workspaces --if-present` clean across all workspaces
- [x] 7.2 Manual: complete a real session (local dev), confirm a `session_mining_results` row is written with all expected fields, and that no mining data appears in the `/end` response or anywhere else the client can see — verified 2026-07-22 against the real Anthropic API (not mocked): full row written with all fields, `/end` response body confirmed to be `{ status: "complete" }` only (see `routes/conversation.ts`). Caught and fixed a real bug in the process — see FIXLIST-worthy note in final report: `output_config.format.schema` rejects `minimum`/`maximum` on an `integer` property (400 from the real API), fixed by dropping those keywords and relying on prompt instructions for the 0-100 range.
- [x] 7.3 Manual: complete a real session linked to an anchor with a recognized `target_role`, confirm `audience_keyword_matches[]` contains real quoted phrases when the transcript contains matching language, and is omitted when it doesn't — verified 2026-07-22: `target_role: "backend engineer"` session produced real verbatim matches ("database schema", "distributed systems", "scalability", "reliability", "throughput").
- [x] 7.4 Manual: complete a real session with no linked anchor, confirm `audience_keyword_matches[]` is absent from the mining result — verified 2026-07-22: no-anchor session's row stored `audienceKeywordMatches: null`.
- [x] 7.5 **Real cost check (not just trusting the original spec estimate):** after production deploy (task 1.4), check the Anthropic Console's real usage against M1–M3's existing per-session cost, and confirm M4's added call keeps total per-session cost under the spec's <$0.05 ceiling; record the actual figure in this task's checkbox notes or the journal — **checked 2026-07-22 by Robin directly in the Console (`platform.claude.com/cost`), filtered to the dedicated `VOCARE_ANTHROPIC...` API key + Claude Haiku 4.5 + Jul 22 range (isolating Vocare's own key from this Claude Code session's separate Sonnet 5 usage, and from an unrelated $1.69 "web search cost" traced to the earlier live-web-search research behind `CRISIS_RESOURCES`, billed under a different key/model): total $0.24 for the entire day, covering all of M2/M3/M4's real-API dev/testing traffic plus the 3 real production sessions combined (this key is shared between local dev and Railway production, per CLAUDE.md's note, so it can't be split further by environment). Even a deliberately generous assumption that a third of that $0.24 came from the 3 production sessions alone gives ~$0.027/session — confirmed independently by an analytical check: Haiku 4.5 pricing ($1/$5 per million input/output tokens) applied to M4's measured 438-token static prompt plus a typical transcript/response adds roughly $0.005/session on top of M2's existing $0.02–0.03 estimate, landing around $0.025–0.035/session. Both the real Console figure and the token-based calculation agree: comfortably under the $0.05 ceiling. Noted for later, not built now: the app has no per-session cost logging of its own, so this check will stay this approximate until that exists — flagged for `FIXLIST.md` if Robin wants it remembered.**
- [x] 7.6 **Caching verification (measurement, not assumption):** measure the mining extraction prompt's static-portion token count once written; record in design.md's Open Questions whether it clears Haiku 4.5's 4096-token cacheable-prefix minimum, and whether `cache_control` was added as a result (default: not added, per design.md's Decisions, unless this measurement shows a clear win) — measured 438 tokens via the real `countTokens` endpoint, below the 4096 minimum; no `cache_control` added. Recorded in design.md's Open Questions.
- [x] 7.7 Confirm crisis-safety-net and live conversation behavior (M2/M3) are unaffected: run an existing session end-to-end, confirm no change to conversational replies, crisis checks, or transcript persistence (no code changes expected in those paths — verification only) — confirmed via the full backend test suite (68 tests, including `crisis-safety.test.ts` and all of `conversation.test.ts`) passing unchanged, plus lint/typecheck/build clean across every workspace.
