## 1. Infrastructure prerequisites

- [x] 1.1 Start Docker Desktop and stand up local Postgres (`postgres:16-alpine`, host port 5433, per CLAUDE.md's Project Facts) via a new `docker-compose.yml` — **corrected 2026-07-22 to port 5434**: 5433 turned out to already be in use by a native PostgreSQL 17 Windows service, not just 18 as originally accounted for; see CLAUDE.md's Project Facts
- [x] 1.2 Provision `api.vocare.ca`: Railway custom domain + DNS record at the registrar (Web Hosting Canada), matching the apex-canonical setup already used for `vocare.ca` — done by Robin directly; confirmed via `railway domain status`: CNAME propagated and verified, TLS cert was still issuing at time of check (normal, transient)
- [x] 1.3 Set up Resend sending and verify domain ownership (SPF/DKIM records at the registrar) — **corrected 2026-07-22 per CLAUDE.md: verifying the apex `vocare.ca` itself, not a separate sending subdomain**; `send-magic-link.ts` already defaults `from` to `hello@vocare.ca` accordingly. Domain verified in Resend 2026-07-21; click tracking left disabled (magic-link-safety reason, not a spec item — see design.md if this needs documenting).
- [x] 1.4 Create the $29 **USD** one-time Stripe Product/Price (currency decided 2026-07-22 — see spec Section 2's Payments row; test mode first), set a recognizable statement descriptor (product name, not a generic business name) — product "Vocare - Lifetime Access" created, live account verified, statement descriptor set to `VOCARE.CA`, product copied to live
- [x] 1.5 Register the Stripe webhook endpoint (test mode) pointing at `api.vocare.ca/webhooks/stripe`, subscribed to `checkout.session.completed` and `charge.dispute.created` — registered via Stripe CLI against the Vocare test-mode account (`we_1TvqV7PRMsESmhhZwEGFsonn`), signing secret wired into Railway (see 1.6)
- [x] 1.6 Add new env vars to local `.env` and Railway: `DATABASE_URL`, `BETTER_AUTH_SECRET`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `RESEND_API_KEY` — local `.env` done throughout this session; Railway done via CLI (`railway variable set ... --service vocare --skip-deploys`) after also provisioning a Railway Postgres instance (none existed — flagged and confirmed with Robin first), using its internal `postgres.railway.internal` URL for `DATABASE_URL`. **Not done as part of this: actually deploying M1's code to Railway or running the migration against the production Postgres** — the env vars are wired and ready, but the currently-running Railway deployment is still M0's hello-world app; a real production rollout of money-handling code is a bigger, separate decision than "set env vars" and deliberately left for `chat`/Robin to decide explicitly, not assumed here.

## 2. Database schema

- [x] 2.1 Add Drizzle ORM + `drizzle-kit` to `@vocare/backend`, configure against `DATABASE_URL`
- [x] 2.2 Define `users` table: id, email, `entitlement_status`, `date_of_birth`, `country`, `paid_at`, timestamps — implemented as additional fields on Better Auth's own generated `user` table (single source of identity), not a second parallel table; see 2.5
- [x] 2.3 Define `sessions` table (practice sessions, not auth sessions): id, `user_id`, `status` (start/in-progress/complete), `crisis_flagged`, `anchor_id` (nullable FK placeholder for M6), `created_at`, `completed_at`
- [x] 2.4 Define `stripe_webhook_events` table: `event_id` (unique), `processed_at`
- [x] 2.5 Add Better Auth's own required tables (auth sessions/verification tokens) via its Drizzle adapter — generated via `@better-auth/cli generate`; real table names confirmed as `user`, `session`, `account`, `verification` (matches design.md's guess, now verified not assumed)
- [x] 2.6 Generate and apply the initial migration against local Postgres; confirm all tables exist

## 3. Magic-link auth

- [x] 3.1 Install and configure Better Auth with the magic-link plugin, Drizzle adapter, no password/OAuth providers enabled (`emailAndPassword.enabled: false`, `socialProviders: {}`)
- [x] 3.2 Implement `sendMagicLink(email, url)` using the Resend SDK — implemented, real send blocked on Robin's Resend account/API key (see task 1.3)
- [x] 3.3 Configure session expiration: 30-day sliding window, refreshed on activity (`expiresIn: 30d`, `updateAge: 1d`)
- [x] 3.4 Build sign-up/sign-in screens per `mockups/interface-v1.html`'s sign-up screen (no password field) — one combined screen (`SignUpPage.tsx`), matching the mockup's single-screen approach for both new and returning users
- [x] 3.5 Add date-of-birth field to sign-up; enforce the configured minimum-age floor server-side before account creation — **deviates from the mockup's checkbox** per design.md's explicit decision (real DOB capture, not a boolean), enforced in `/auth/pending-signup` before any magic link is even sent
- [x] 3.6 Add country field to sign-up, pre-filled from browser locale, editable
- [x] 3.7 Unit tests: magic-link issuance/verification, session sliding expiration, age-gate rejection below floor — `src/auth/auth.test.ts`, 5 tests, run as real integration tests against local Postgres (not mocked), all passing

## 4. Stripe billing

- [x] 4.1 Create a checkout-session route (web-only) that creates a Stripe Checkout session (`mode: "payment"`, $29 price from 1.4) — `POST /billing/checkout-session`, reads `STRIPE_PRICE_ID`; real end-to-end call still blocked on Robin handing over the new Vocare account's test-mode secret key + price ID (see task 1.6)
- [x] 4.2 Create the webhook route as its own encapsulated Fastify plugin with a scoped raw-body content-type parser (per design.md — must not affect any other route's JSON parsing)
- [x] 4.3 Implement `checkout.session.completed` handling: verify signature, insert `event.id` into `stripe_webhook_events` (idempotency), flip `entitlement_status` to paid, set `paid_at` — all in one transaction. Added a small `stripe_payments` table (payment_intent_id → user_id) not explicitly named in design.md's schema list — needed so the dispute handler below can trace a dispute back to a user, since dispute events only carry the charge/PaymentIntent, not our userId.
- [x] 4.4 Implement `charge.dispute.created` handling: verify signature, insert `event.id` idempotently, revoke `entitlement_status` back to unpaid — via the `stripe_payments` mapping above
- [x] 4.5 Reject requests with missing/invalid Stripe signatures without touching `entitlement_status`
- [x] 4.6 Unit tests: signature verification (valid/invalid), idempotent replay of the same `event.id`, dispute revocation — `src/billing/webhook.test.ts`, 5 tests, real integration tests against local Postgres using `stripe.webhooks.generateTestHeaderString` (no live Stripe account needed for signature-verification tests), all passing
- [x] 4.7 Paywall UI per `mockups/interface-v1.html`'s paywall screen; no-refund policy stated in ToS copy (placeholder legal text, flagged not final)

## 5. Session entitlement

- [x] 5.1 Implement a server-side entitlement-check function called at every session start — never trusts a client-supplied entitlement value (`checkEntitlement(userId)` in `src/entitlement/entitlement.ts`, takes only a userId, no client-supplied flag exists to spoof)
- [x] 5.2 Implement the free-session count as a query over completed, non-crisis-flagged `sessions` rows (not a mutable counter column), gating session start at 3
- [x] 5.3 Implement the velocity cap as rolling 24h/30-day `COUNT(*)` queries over `sessions.created_at`, excluding crisis-flagged rows, gating session start against placeholder threshold constants
- [x] 5.4 Add the plain in-app limit message for a tripped velocity cap, worded to avoid committing to a specific reset window (per design.md's two-window caveat) — `VELOCITY_CAP_MESSAGE`, tested for absence of "tomorrow"/window-specific wording
- [x] 5.5 Add the Android entitlement-check call path (server verification only, no purchase UI) for future M10 use — `GET /entitlement/check` is a single platform-agnostic REST route; no Android-specific code exists yet since the app shell itself is M10 (per design.md's Non-Goals), but the same endpoint is what it will call
- [x] 5.6 Unit tests: entitlement denial on cached-but-stale client flag, free-session gate at the 3-session boundary, abandoned-session non-decrement, crisis-exemption for both the free-session count and the velocity cap, velocity-cap trip message — `src/entitlement/entitlement.test.ts`, 8 tests, all passing against real Postgres

## 6. Account management

- [x] 6.1 Build the profile/account screen (email, entitlement status text, deletion trigger) per `mockups/interface-v1.html`'s profile screen — `AccountPage.tsx`, backed by `GET /account/me`; verified rendering correctly in a real browser (see 7.2's note)
- [x] 6.2 Implement `deleteUserCascade(userId)` as the single owned function performing an explicit, ordered, transactional delete across `users`, `sessions`, `transcript_turns`, `session_mining_results`, `feedback_reports`, `tier1_profiles`, `anchors`, `anchor_revisions`, **and Better Auth's own session/verification-token tables** (design.md's decision — no `ON DELETE CASCADE` FKs). Tables that exist as of M1 (`user`/`session`/`account`/`verification`, practice `sessions`, `stripe_payments`) are deleted now; the six tables from later modules that don't exist yet are called out in a comment as the extension point for whichever module creates them. `verification` rows are matched by email (Better Auth keys them by token, not userId) so no stale magic-link token survives deletion.
- [x] 6.3 Wire the profile screen's deletion trigger to a confirm step, then to `deleteUserCascade` — `DELETE /account`
- [x] 6.4 Unit tests: deletion removes rows from every listed table for a seeded user with data in each; deletion leaves other users' rows untouched — `src/account/delete-user-cascade.test.ts`, 2 tests, passing against real Postgres

## 7. Verification

- [x] 7.1 `npm run lint|typecheck|test|build --workspaces --if-present` clean — all four commands, all four workspaces (shared/backend/web/mobile), exit 0; 23 automated tests passing total
- [x] 7.2 Manual: sign up with a real email, receive and click a real magic-link email via Resend, land authenticated — Robin received and clicked a real Resend-delivered email to rgsamways@gmail.com, landed authenticated on `/account` with correct data. **Bug found and fixed:** the backend log showed the same token verified twice ~3 minutes apart, both from `127.0.0.1` — the first (silent) hit consumed the single-use token and set a valid session cookie; Robin's actual click 3 minutes later correctly got `INVALID_TOKEN` for the already-used token, but landed on a working account page anyway since the session from the first hit was still valid. Given both hits were local, this wasn't Gmail's server-side link scanner (no route to `localhost`) — most likely Chrome's local link-prefetch/preview while reading the email on the same machine as the dev server. Better Auth's single-use token enforcement worked correctly; the actual bug was `AccountPage.tsx` leaving a stale `?error=INVALID_TOKEN` in the address bar despite a valid session existing. Fixed with an effect that strips the query string via `navigate("/account", { replace: true })` once a session is confirmed. Verified in a real browser: reproduced the exact double-verify scenario, confirmed the URL shows the error immediately then cleans to a bare `/account` once the session resolves, with the account data intact throughout.
- [x] 7.3 Manual: complete a real Stripe test-mode checkout end to end, confirm `entitlement_status` flips via the actual webhook (not a mocked call), using the Stripe CLI to forward test webhooks locally — Robin ran `stripe login` to point the CLI at the new Vocare test-mode account; used `stripe listen --forward-to localhost:3000/webhooks/stripe` for real forwarding, drove the actual hosted Stripe Checkout page (confirmed "Vocare - Lifetime Access", "US$29.00", Sandbox badge) with test card 4242 4242 4242 4242 via a real browser. `checkout.session.completed` arrived through the real webhook and flipped `entitlement_status` to `paid` with `paid_at` set.
- [x] 7.4 Manual: trigger a test-mode dispute via the Stripe CLI/dashboard, confirm `entitlement_status` reverts — repeated the same real checkout using Stripe's dispute-simulation test card (4000000000000259); the real `charge.dispute.created` webhook arrived immediately after `checkout.session.completed` and correctly reverted `entitlement_status` back to `unpaid` (with `paid_at` left as-is from the original grant) — the full grant-then-revoke cycle confirmed against real Stripe events, not mocked.
- [x] 7.5 Manual: exhaust 3 free sessions, confirm the 4th is blocked; confirm an abandoned session didn't decrement the count — done against the live dev server + real Postgres (not just the unit tests): seeded a real user, hit `GET /entitlement/check` with a real session cookie before/after 3 completed sessions (`true` → `false, paywall`), then added a 4th `start`-status (abandoned) session and confirmed it stayed blocked, not further changed
- [x] 7.6 Manual: seed a user past the placeholder velocity-cap threshold, confirm the in-app limit message appears and wording doesn't overcommit on reset timing — same live-server approach, seeded a **paid** user with 6 sessions in 24h (cap is `per24h: 6`) to also confirm the cap applies regardless of paid status; got back `{"allowed":false,"reason":"velocity_cap","message":"You've reached your practice limit for now — check back soon."}`
- [x] 7.7 Manual: delete a seeded test account, confirm no rows remain in any of the eight tables and old session/magic-link tokens no longer authenticate — done twice: once through the real browser UI (sign-up → verify → account → delete → redirected to `/`, `psql` confirmed 0 rows), once via direct API calls confirming the *same pre-deletion session cookie* gets `401` afterward. Bonus finding: a deleted identity can't even be resurrected via a brand-new magic-link request for the same email — `databaseHooks.user.create.before` blocks it (`error=failed_to_create_user`) since no `pending_signups` row survives deletion. Not explicitly required by the spec scenario but a reasonable extra safety property, not a bug.
- [x] 7.8 Hand off to `chat` for independent grading against `proposal.md`/`design.md`/`tasks.md` per CLAUDE.md's two-instance process — `chat` reads only these persisted files, not this session's conversation. Handing off now: 44/44 tasks checked off, `openspec validate m1-auth-entitlement --strict` passes.
